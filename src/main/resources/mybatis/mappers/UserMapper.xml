<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="garin.javaredis.dao.UserMapper">


    <select id="queryAll" resultType="garin.javaredis.model.User">
        select * from t_user
    </select>
    <select id="findUserById" resultType="garin.javaredis.model.User">
        select * from t_user where userId=#{id}
    </select>
    <update id="updateUser">

    </update>
    <delete id="deleteUserById">

    </delete>
    <insert id="insertUser">

    </insert>
</mapper>